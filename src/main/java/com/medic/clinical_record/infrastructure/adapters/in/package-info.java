/**
 * Inbound infrastructure adapters — adapters that drive the application from an infrastructure trigger.
 *
 * <p>Examples: message broker consumers (RabbitMQ, Kafka listeners), webhook receivers.
 * REST controllers driven by HTTP requests are placed in {@code presentation.controllers} instead.
 */
package com.medic.clinical_record.infrastructure.adapters.in;
