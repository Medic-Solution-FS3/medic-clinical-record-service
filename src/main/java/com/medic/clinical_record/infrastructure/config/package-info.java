/**
 * Infrastructure configuration — Spring {@code @Configuration} classes that wire the application together.
 *
 * <p>This is where dependency injection bindings are declared manually: binding output port
 * interfaces to their concrete adapter implementations, configuring beans for external clients, etc.
 */
package com.medic.clinical_record.infrastructure.config;
