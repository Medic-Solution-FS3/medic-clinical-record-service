/**
 * Application services — implementations of input ports that orchestrate domain logic.
 *
 * <p>Service classes implement the use case interfaces defined in {@code application.ports.in}
 * and depend on the output port interfaces from {@code application.ports.out}.
 * They must not depend on infrastructure or presentation details.
 */
package com.medic.clinical_record.application.services;
