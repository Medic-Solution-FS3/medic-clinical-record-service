/**
 * Output ports (driven ports) — interfaces that the application uses to interact with the outside world.
 *
 * <p>Examples: repository interfaces, event publisher interfaces, external API client interfaces.
 * Concrete implementations live in {@code infrastructure.adapters.out}, not here.
 */
package com.medic.clinical_record.application.ports.out;
