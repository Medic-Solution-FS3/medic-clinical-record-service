/**
 * Input ports (driving ports) — interfaces that define the use cases exposed to the outside world.
 *
 * <p>These interfaces represent what the application can do. Inbound adapters (REST controllers,
 * event listeners) depend on these interfaces, never on concrete implementations.
 */
package com.medic.clinical_record.application.ports.in;
